# Candidate

This deliberately empty workspace belongs to the agent. It must leave an
executable `.alo/run`, all required boot artifacts, source or build scripts,
lock data, and useful notes.

The replay must be self-contained. Workshop-only files and state cannot satisfy
verification. The installed GRUB configuration and its default boot entry must
remain unchanged.
